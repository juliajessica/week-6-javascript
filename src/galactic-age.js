class galacticAge {
  constructor(age) {
    this.age = age;
  }

  // getSeconds(userAge) {
  //   let ageToSeconds = userAge;
  // } return ageToSeconds.toFixed();
} //closes class

export { galacticAge };
